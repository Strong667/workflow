# Базовый URL API. В dev-режиме запросы идут через прокси Vite на бэкенд.
VITE_API_URL=/api

# Куда Vite проксирует /api. По умолчанию http://127.0.0.1:8000 —
# задавайте явно, если Laravel поднят на другом порту.
VITE_API_PROXY=http://127.0.0.1:8000
