# Базовый URL API для axios. В dev-режиме запросы идут через прокси Vite.
VITE_API_URL=/api

# Порт dev-сервера фронтенда (по умолчанию 5188).
VITE_PORT=5188

# Куда Vite проксирует /api — адрес Laravel (по умолчанию http://127.0.0.1:8088).
VITE_API_PROXY=http://127.0.0.1:8088
